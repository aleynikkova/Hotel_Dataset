# 🚀 ЗАПУСК ПРОЕКТА

## ✅ ПРОЕКТ ЗАПУЩЕН!

### 🌐 Доступные URL:

**Frontend (интерфейс пользователя):**
- 🏠 Главная страница: http://localhost:3000
- 📝 Регистрация/Вход: http://localhost:3000

**Backend (API):**
- 📚 Swagger документация: http://localhost:8000/docs
- 🔍 ReDoc: http://localhost:8000/redoc
- 🔗 API endpoint: http://localhost:8000/api/v1/

---

## 👤 ТЕСТОВЫЕ АККАУНТЫ

### Гость (guest):
- **Email:** test.guest@test.ru
- **Пароль:** password123

### Администратор отеля (hotel_admin):
- **Email:** admin.mountain@hotel.ru
- **Пароль:** admin123

### Другие аккаунты:
- nastya.yes@list.ru / admin123
- dimukhametovan@mail.ru / admin123

---

## 🛑 ОСТАНОВКА СЕРВЕРОВ

```bash
# Остановить Backend
lsof -ti:8000 | xargs kill -9

# Остановить Frontend  
lsof -ti:3000 | xargs kill -9

# Остановить оба
lsof -ti:8000,3000 | xargs kill -9
```

---

## 🔄 ПЕРЕЗАПУСК

```bash
# Backend
cd ~/Desktop/КП_БД/backend
source venv/bin/activate
PYTHONPATH=$PWD uvicorn app.main:app --reload --host 0.0.0.0 --port 8000

# Frontend (в новом терминале)
cd ~/Desktop/КП_БД/frontend
python3 -m http.server 3000
```

---

## 📊 ПРОВЕРКА СТАТУСА

```bash
# Проверить что серверы запущены
lsof -ti:8000 && echo "✅ Backend работает" || echo "❌ Backend НЕ запущен"
lsof -ti:3000 && echo "✅ Frontend работает" || echo "❌ Frontend НЕ запущен"

# Логи Backend
tail -f /tmp/backend.log

# Логи Frontend
tail -f /tmp/frontend.log
```

---

## 🗄️ БАЗА ДАННЫХ

**Подключение:**
- Host: localhost
- Port: 5432
- Database: postgres
- User: postgres
- Password: Mazda220505

**Пересоздание БД:**
```bash
cd ~/Desktop/КП_БД
./setup_database.sh
```

---

## 🎯 ОСНОВНЫЕ ФУНКЦИИ

1. **Регистрация** — создать новый аккаунт гостя
2. **Поиск отелей** — найти и забронировать номер
3. **Мои бронирования** — просмотр своих броней
4. **Отзывы** — оставить отзыв после проживания
5. **Админ-панель** — управление отелем (для hotel_admin)

---

**Дата запуска:** $(date +"%d.%m.%Y %H:%M")
