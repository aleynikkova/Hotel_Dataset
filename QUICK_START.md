# 🏨 Руководство по запуску проекта

## Быстрый старт (для защиты КП)

### 1. Подготовка базы данных

```bash
# Создайте базу данных в PostgreSQL
createdb -U postgres hotel_booking

# Если у вас уже есть данные из DBeaver, выполните миграцию:
psql -U postgres -d hotel_booking -f database/01_migration_add_users.sql
psql -U postgres -d hotel_booking -f database/02_triggers.sql
psql -U postgres -d hotel_booking -f database/03_views.sql
psql -U postgres -d hotel_booking -f database/04_functions.sql

# Если нужны дополнительные тестовые данные:
psql -U postgres -d hotel_booking -f database/05_test_data.sql
```

**Тестовые пользователи после миграции:**
- **Системный админ**: `admin@hotel.com` / `admin123`
- **Админ отеля**: `admin.plaza@hotel.ru` / `hotel123`
- **Гости**: любой email из вашей таблицы guests / `password123`

### 2. Запуск Backend (FastAPI)

```bash
cd backend

# Создайте виртуальное окружение
python3 -m venv venv
source venv/bin/activate  # Mac/Linux
# или: venv\Scripts\activate  # Windows

# Установите зависимости
pip install -r requirements.txt

# Настройте .env файл
cp .env.example .env
# Отредактируйте .env: укажите свои данные PostgreSQL

# Запустите сервер
python -m uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

Backend будет доступен на `http://localhost:8000`
API документация (Swagger): `http://localhost:8000/docs`

### 3. Запуск Frontend

```bash
# Откройте новый терминал в папке frontend
cd frontend

# Запустите простой HTTP сервер
python3 -m http.server 3000

# Или используйте любой другой веб-сервер
```

Frontend будет доступен на `http://localhost:3000`

### 4. Проверка работы

1. Откройте `http://localhost:3000` в браузере
2. Нажмите "Войти" и используйте тестового пользователя
3. Проверьте список отелей
4. Проверьте функционал в зависимости от роли

## 📋 Что реализовано для оценки 4

### ✅ База данных (PostgreSQL)
- [x] Таблицы с правильной структурой и связями
- [x] Таблица `users` с `password_hash` (bcrypt)
- [x] Роли пользователей: guest, hotel_admin, system_admin
- [x] **Триггеры** (автоматический расчет цены, проверка доступности, логирование)
- [x] **Функции** (расчет стоимости, поиск номеров, статистика, отчеты)
- [x] **Представления** (views для удобных выборок)
- [x] **Хранимые процедуры** (отмена просроченных бронирований)

### ✅ Backend (FastAPI)
- [x] RESTful API на Python
- [x] Асинхронная работа с БД (asyncpg)
- [x] JWT аутентификация
- [x] Хеширование паролей (bcrypt)
- [x] Разграничение прав по ролям
- [x] Обработка ошибок
- [x] Параметризованные SQL запросы (защита от инъекций)
- [x] Вся бизнес-логика на стороне Backend

### ✅ Frontend (Web-приложение)
- [x] HTML5 + CSS3 + JavaScript (ES6)
- [x] Bootstrap 5 для UI
- [x] Асинхронные запросы (Fetch API)
- [x] Авторизация по логину/паролю
- [x] Интерфейс для разных ролей
- [x] Регистрация новых пользователей
- [x] Адаптивный дизайн

### ✅ Функционал по ролям

**Гость:**
- Регистрация и вход
- Просмотр отелей
- Просмотр номеров
- Создание бронирований (в процессе доработки)
- Просмотр своих бронирований
- Добавление отзывов

**Администратор отеля:**
- Все функции гостя
- Управление номерами своего отеля
- Просмотр бронирований отеля
- Изменение статусов бронирований
- Модерация отзывов

**Системный администратор:**
- Все функции
- Управление пользователями
- Управление отелями
- Назначение администраторов отелям
- Просмотр системных логов

## 🔧 Конфигурация

### Backend (.env)
```env
DATABASE_URL=postgresql+asyncpg://postgres:ВАШ_ПАРОЛЬ@localhost:5432/hotel_booking
SECRET_KEY=сгенерируйте-случайную-строку-минимум-32-символа
ACCESS_TOKEN_EXPIRE_MINUTES=30
```

### Frontend (js/api.js)
```javascript
const API_BASE_URL = 'http://localhost:8000/api/v1';
```

## 🧪 Тестирование

### Проверка API через Swagger:
1. Откройте `http://localhost:8000/docs`
2. Используйте кнопку "Authorize" для входа
3. Тестируйте endpoints

### Проверка БД:
```sql
-- Проверка пользователей
SELECT * FROM users;

-- Проверка представлений
SELECT * FROM v_hotels_full;

-- Проверка функций
SELECT * FROM get_available_rooms(1, '2024-12-20', '2024-12-25', 2);

-- Проверка триггеров (создайте бронирование и проверьте автоматический расчет цены)
```

## 🐛 Troubleshooting

### Ошибка подключения к БД:
- Проверьте, что PostgreSQL запущен
- Проверьте правильность данных в `.env`
- Проверьте, что база данных `hotel_booking` создана

### Ошибка CORS:
- Убедитесь, что backend запущен на порту 8000
- Проверьте настройки CORS в `backend/app/core/config.py`

### Ошибка 401 Unauthorized:
- Проверьте, что токен не истек (по умолчанию 30 минут)
- Попробуйте выйти и войти снова

## 📝 Дополнительная доработка

Если времени достаточно, можно доработать:
1. Полный CRUD для номеров
2. Систему бронирований с проверкой доступности
3. Систему отзывов
4. Формирование отчетов (экспорт в CSV)
5. Админ-панель с графиками

## 📤 Публикация на GitHub

```bash
# Добавьте remote репозиторий (создайте репозиторий на GitHub)
git remote add origin https://github.com/ВАШ_USERNAME/hotel-booking.git

# Отправьте код
git push -u origin main
```

Добавьте в репозиторий сокомандницу:
Settings → Collaborators → Add people

## 🎓 Для защиты КП

**Что демонстрировать:**

1. **Структура БД** - покажите таблицы в DBeaver/pgAdmin
2. **Триггеры** - создайте бронирование и покажите автоматический расчет цены
3. **Функции** - выполните несколько функций в SQL
4. **Представления** - покажите выборки из views
5. **Backend API** - откройте Swagger документацию
6. **Аутентификацию** - покажите вход с разными ролями
7. **Frontend** - покажите работу интерфейса
8. **Разграничение прав** - войдите под разными ролями

**Подготовьте ответы на вопросы:**
- Как хранятся пароли? (Bcrypt hash)
- Как работает аутентификация? (JWT токены)
- Где находится бизнес-логика? (В Backend и триггерах БД)
- Как защищена БД от SQL-инъекций? (Параметризованные запросы через SQLAlchemy)
- Как работают асинхронные запросы? (Async/await, asyncpg)

Удачи на защите! 🚀
