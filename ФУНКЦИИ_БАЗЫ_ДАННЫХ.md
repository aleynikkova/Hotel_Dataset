# Объяснение основных функций и процедур в init_schema.sql

Этот документ содержит подробное описание всех функций, процедур и триггеров базы данных системы управления бронированием отелей.

---

## 📊 Содержание

1. [Функции расчета стоимости](#функции-расчета-стоимости)
2. [Функции проверки доступности](#функции-проверки-доступности)
3. [Функции получения информации](#функции-получения-информации)
4. [Процедуры обслуживания](#процедуры-обслуживания)
5. [Триггерные функции](#триггерные-функции)

---

## Функции расчета стоимости

### 1. `calculate_booking_price(p_room_id, p_check_in, p_check_out)`

**Назначение:** Вычисляет общую стоимость бронирования номера на основе его цены за ночь и количества ночей проживания.

**Параметры:**
- `p_room_id` (integer) - ID номера
- `p_check_in` (date) - Дата заезда
- `p_check_out` (date) - Дата выезда

**Возвращает:** `numeric` - Общая стоимость бронирования

**Логика работы:**
1. Получает цену за ночь из таблицы `roomtypes` через связь с таблицей `rooms`
2. Проверяет существование номера (выдает ошибку, если номер не найден)
3. Вычисляет количество ночей (разница между датами выезда и заезда)
4. Проверяет корректность дат (дата выезда должна быть позже даты заезда)
5. Вычисляет итоговую стоимость: `цена_за_ночь × количество_ночей`

**Пример использования:**
```sql
SELECT calculate_booking_price(5, '2025-12-20', '2025-12-25');
-- Вернет: 15000.00 (если цена номера 3000 руб/ночь × 5 ночей)
```

---

### 2. `calculate_total_price_trigger()`

**Назначение:** Триггерная функция, автоматически вычисляющая стоимость при создании или изменении бронирования.

**Когда срабатывает:** 
- BEFORE INSERT на таблице `bookings`
- BEFORE UPDATE при изменении полей `room_id`, `check_in_date`, `check_out_date`

**Логика работы:**
1. Получает цену номера из связанной таблицы `roomtypes`
2. Вычисляет количество ночей
3. Проверяет корректность дат
4. Автоматически заполняет поле `NEW.total_price` в записи бронирования

**Преимущества:**
- Гарантирует актуальность стоимости в любой момент
- Исключает ошибки ручного ввода
- Обеспечивает консистентность данных

---

## Функции проверки доступности

### 3. `check_booking_availability()`

**Назначение:** Триггерная функция, проверяющая возможность создания бронирования (отсутствие конфликтов с существующими бронированиями).

**Когда срабатывает:**
- BEFORE INSERT на таблице `bookings`
- BEFORE UPDATE при изменении полей `room_id`, `check_in_date`, `check_out_date`

**Логика работы:**
1. Проверяет пересечения с существующими бронированиями:
   - Исключает отмененные бронирования (`status != 'cancelled'`)
   - Проверяет три типа пересечений дат:
     - Заезд попадает в существующее бронирование
     - Выезд попадает в существующее бронирование
     - Новое бронирование полностью перекрывает существующее
2. Проверяет, что номер доступен для бронирования (`is_available = TRUE`)
3. Выдает ошибку при обнаружении конфликтов

**Защищает от:**
- Двойного бронирования одного номера
- Бронирования недоступных номеров
- Конфликтующих дат

---

### 4. `check_room_availability(p_room_id, p_check_in, p_check_out, p_exclude_booking_id)`

**Назначение:** Проверяет доступность конкретного номера в указанный период (может использоваться в API).

**Параметры:**
- `p_room_id` (integer) - ID номера для проверки
- `p_check_in` (date) - Дата начала периода
- `p_check_out` (date) - Дата окончания периода
- `p_exclude_booking_id` (integer, optional) - ID бронирования, которое нужно исключить из проверки (для UPDATE)

**Возвращает:** `boolean` - TRUE если номер доступен, FALSE если занят

**Логика работы:**
1. Подсчитывает количество конфликтующих бронирований
2. Исключает отмененные бронирования
3. Исключает указанное бронирование (если передан `p_exclude_booking_id`)
4. Проверяет все виды пересечений дат

**Пример использования:**
```sql
-- Проверить, свободен ли номер 101 с 20 по 25 декабря
SELECT check_room_availability(101, '2025-12-20', '2025-12-25');
```

---

## Функции получения информации

### 5. `get_available_rooms(p_check_in, p_check_out, p_hotel_id)` - Версия 1

**Назначение:** Возвращает список всех доступных номеров в указанный период, опционально фильтруя по отелю.

**Параметры:**
- `p_check_in` (date) - Дата заезда
- `p_check_out` (date) - Дата выезда
- `p_hotel_id` (integer, optional) - ID отеля (NULL = все отели)

**Возвращает таблицу с полями:**
- `room_id` - ID номера
- `hotel_id` - ID отеля
- `hotel_name` - Название отеля
- `roomtype_id` - ID типа номера
- `type_name` - Название типа номера
- `room_number` - Номер комнаты
- `floor` - Этаж
- `price_per_night` - Цена за ночь
- `is_available` - Доступность номера

**Логика работы:**
1. Объединяет таблицы `rooms`, `hotels`, `roomtypes`
2. Фильтрует по `is_available = TRUE`
3. Исключает номера с конфликтующими бронированиями (статусы 'confirmed', 'checked_in')
4. Применяет фильтр по отелю, если указан

---

### 6. `get_available_rooms(p_hotel_id, p_check_in, p_check_out)` - Версия 2

**Назначение:** Перегруженная версия функции для конкретного отеля с расчетом общей стоимости.

**Параметры:**
- `p_hotel_id` (integer) - ID отеля (обязательный)
- `p_check_in` (date) - Дата заезда
- `p_check_out` (date) - Дата выезда

**Возвращает таблицу с полями:**
- `room_id` - ID номера
- `room_number` - Номер комнаты
- `type_name` - Тип номера
- `price_per_night` - Цена за ночь
- `total_price` - Общая стоимость за весь период

**Отличия от версии 1:**
- Фокус на одном отеле
- Сразу вычисляет общую стоимость
- Компактный вывод только необходимой информации

---

### 7. `get_hotel_average_rating(p_hotel_id)`

**Назначение:** Вычисляет средний рейтинг отеля на основе всех отзывов.

**Параметры:**
- `p_hotel_id` (integer) - ID отеля

**Возвращает:** `numeric(3,2)` - Средний рейтинг (от 0.00 до 5.00)

**Логика работы:**
1. Объединяет таблицы `reviews`, `bookings`, `rooms`
2. Фильтрует отзывы по номерам конкретного отеля
3. Вычисляет среднее значение рейтинга
4. Округляет до 2 знаков после запятой
5. Возвращает 0, если отзывов нет

**Пример использования:**
```sql
SELECT get_hotel_average_rating(1);
-- Вернет: 4.35
```

---

### 8. `get_hotel_occupancy_stats(p_hotel_id, p_start_date, p_end_date)`

**Назначение:** Возвращает статистику заполненности отеля за указанный период.

**Параметры:**
- `p_hotel_id` (integer) - ID отеля
- `p_start_date` (date) - Начало периода
- `p_end_date` (date) - Конец периода

**Возвращает таблицу:**
- `total_rooms` - Общее количество номеров
- `booked_rooms` - Количество забронированных номеров
- `available_rooms` - Количество свободных номеров
- `occupancy_rate` - Процент заполненности (%)

**Логика работы:**
1. Подсчитывает общее количество доступных номеров отеля
2. Подсчитывает уникальные номера с активными бронированиями (статусы 'confirmed', 'checked_in')
3. Проверяет пересечение бронирований с указанным периодом
4. Вычисляет процент заполненности: `(забронированные / общие) × 100`

**Пример использования:**
```sql
SELECT * FROM get_hotel_occupancy_stats(1, '2025-12-01', '2025-12-31');
-- Результат:
-- total_rooms | booked_rooms | available_rooms | occupancy_rate
-- 50          | 35           | 15              | 70.00
```

---

## Процедуры обслуживания

### 9. `cancel_expired_bookings()`

**Назначение:** Автоматическая отмена просроченных бронирований (техническое обслуживание БД).

**Параметры:** Нет

**Логика работы:**
1. Находит все бронирования со статусом 'pending'
2. Проверяет, что дата заезда уже прошла (`check_in_date < CURRENT_DATE`)
3. Изменяет статус на 'cancelled'
4. Выводит уведомление с количеством отмененных бронирований

**Когда использовать:**
- В регулярных задачах cron (например, ежедневно в 00:00)
- При ручном обслуживании базы данных
- Перед генерацией отчетов

**Пример вызова:**
```sql
CALL cancel_expired_bookings();
-- Вывод: NOTICE: Отменено 5 просроченных бронирований
```

---

## Триггерные функции

### 10. `set_booking_date()`

**Назначение:** Автоматически устанавливает дату создания бронирования.

**Когда срабатывает:** BEFORE INSERT на таблице `bookings`

**Логика работы:**
1. Проверяет, не установлена ли уже дата бронирования
2. Если `booking_date` NULL, устанавливает текущую дату и время (`CURRENT_TIMESTAMP`)

**Преимущества:**
- Гарантирует заполнение поля `booking_date`
- Точная временная метка создания записи
- Не требует явного указания при INSERT

---

### 11. `validate_review_rights()`

**Назначение:** Проверяет права пользователя на добавление отзыва и корректность данных.

**Когда срабатывает:** BEFORE INSERT на таблице `reviews`

**Логика работы:**
1. **Проверка существования бронирования:**
   - Получает `user_id` и статус бронирования
   - Выдает ошибку, если бронирование не найдено

2. **Проверка статуса бронирования:**
   - Разрешает отзывы только для завершенных проживаний (`status = 'completed'`)
   - Блокирует отзывы на активные или отмененные бронирования

3. **Проверка уникальности отзыва:**
   - Проверяет, что для этого бронирования еще не был оставлен отзыв
   - Исключает текущий отзыв при UPDATE (по `review_id`)

4. **Автоматическая дата отзыва:**
   - Если `review_date` не указана, устанавливает текущую дату и время

**Бизнес-правила:**
- Один отзыв на одно бронирование
- Отзыв только после завершения проживания
- Автоматическая временная метка

**Пример ошибок:**
```sql
-- Попытка добавить отзыв на незавершенное бронирование:
INSERT INTO reviews (booking_id, rating, comment) VALUES (123, 5, 'Отлично!');
-- ERROR: Отзыв можно оставить только после завершения проживания

-- Попытка добавить второй отзыв:
INSERT INTO reviews (booking_id, rating, comment) VALUES (456, 4, 'Хорошо');
-- ERROR: Отзыв на это бронирование уже существует
```

---

## 🎯 Общая архитектура

### Принципы проектирования:

1. **Автоматизация:** Триггеры автоматически вычисляют цены, даты и проверяют ограничения
2. **Валидация данных:** Все критичные операции проходят проверку на уровне БД
3. **Бизнес-логика в БД:** Правила бронирования и отзывов инкапсулированы в функциях
4. **Предотвращение конфликтов:** Система исключает двойное бронирование
5. **Консистентность:** Триггеры гарантируют актуальность расчетных данных

### Соответствие 3NF (Третья нормальная форма):

- Цена номера хранится в `roomtypes`, а не дублируется в `rooms`
- `calculate_booking_price` получает цену через JOIN, обеспечивая актуальность
- Отсутствие транзитивных зависимостей

---

## 📝 Рекомендации по использованию

1. **При создании бронирования:** 
   - Не нужно вручную указывать `total_price` - рассчитается автоматически
   - Не нужно указывать `booking_date` - установится текущая дата

2. **При проверке доступности:**
   - Используйте `check_room_availability()` для быстрой проверки
   - Используйте `get_available_rooms()` для получения списка вариантов

3. **При работе с отзывами:**
   - Убедитесь, что бронирование завершено (`status = 'completed'`)
   - Система автоматически проверит права на добавление отзыва

4. **Регулярное обслуживание:**
   - Запускайте `cancel_expired_bookings()` ежедневно через cron или pg_cron

---

## 🔍 Типичные SQL-запросы с использованием функций

### Проверить доступность номера:
```sql
SELECT check_room_availability(101, '2025-12-25', '2025-12-30');
```

### Получить список доступных номеров:
```sql
SELECT * FROM get_available_rooms('2025-12-25', '2025-12-30', 1);
```

### Рассчитать стоимость бронирования:
```sql
SELECT calculate_booking_price(15, '2025-12-25', '2025-12-30');
```

### Получить статистику отеля:
```sql
SELECT * FROM get_hotel_occupancy_stats(1, '2025-12-01', '2025-12-31');
```

### Получить рейтинг отеля:
```sql
SELECT get_hotel_average_rating(1);
```

---

**Дата создания документации:** 19 декабря 2025 г.  
**Версия схемы:** init_schema.sql
